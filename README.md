# 我的工具箱 - 落地页

一个现代化的工具产品展示落地页，基于 Cloudflare Workers 部署，包含产品展示、详情页面和联系表单等功能。

## 🚀 功能特点

- 📱 **响应式设计** - 完美适配桌面端和移动端
- ⚡ **Cloudflare Workers 部署** - 全球加速，零维护成本
- 🎨 **现代UI设计** - 简洁美观的用户界面
- 📦 **产品展示** - 支持产品卡片展示和详情页面
- 🔧 **开源友好** - 包含 AI 面包表情生成器等开源项目
- 📧 **联系表单** - 集成联系表单功能
- 🛠️ **易于定制** - 模块化设计，易于扩展和修改

## 📋 展示产品

### 🤖 AI 面包表情生成器
- **GitHub**: https://github.com/liangdabiao/ai-make-face-meme
- **描述**: 基于 AI 技术的面部表情包生成工具
- **功能**:
  - AI 智能面部识别和表情生成
  - 多种表情包风格模板
  - 自定义文字和贴纸
  - 高质量图片输出
  - 开源免费使用

### 📝 文本转换器
- 支持多种文本格式转换
- 编码解码功能
- JSON格式化和验证

### 🎨 颜色选择器
- 专业颜色选择工具
- 多格式颜色转换
- 渐变色生成器

### 📱 二维码生成器
- 多种内容类型支持
- 自定义样式和Logo
- 批量生成功能

### 🔐 密码生成器
- 安全密码生成
- 强度评估和检测
- 批量生成功能

### 🖼️ 图片压缩器
- 多格式支持
- 智能质量压缩
- 批量处理功能

### {} JSON格式化工具
- JSON格式化和美化
- 语法验证和错误提示
- API调试功能

## 🛠️ 技术栈

- **前端**: HTML5, CSS3, Vanilla JavaScript
- **部署**: Cloudflare Workers
- **构建工具**: Wrangler CLI
- **样式**: CSS Variables, Flexbox, Grid
- **交互**: DOM API, Fetch API

## 📦 安装和部署

### 1. 环境准备

确保你已安装 Node.js (版本 >= 16)：

```bash
node --version
```

### 2. 安装 Wrangler CLI

```bash
npm install -g wrangler
```

### 3. 克隆项目

```bash
git clone <repository-url>
cd cfpage
```

### 4. 安装依赖

```bash
npm install
```

### 5. 登录 Cloudflare

```bash
wrangler login
```

### 6. 配置项目

编辑 `wrangler.toml` 文件，修改域名和配置：

```toml
name = "your-worker-name"
main = "worker.js"
compatibility_date = "2024-01-01"

[env.production]
name = "your-worker-name"
routes = [
  { pattern = "your-domain.com/*", zone_name = "your-domain.com" }
]
```

### 7. 本地开发

```bash
npm run dev
```

访问 `http://localhost:8787` 查看本地版本。

### 8. 部署到 Cloudflare Workers

#### 测试环境部署
```bash
npm run deploy:staging
```

#### 生产环境部署
```bash
npm run deploy:production
```

## 📁 项目结构

```
cfpage/
├── index.html          # 主页模板
├── styles.css          # 样式文件
├── script.js           # 前端交互脚本
├── worker.js           # Cloudflare Workers 主脚本
├── wrangler.toml       # Wrangler 配置文件
├── package.json        # 项目依赖配置
└── README.md          # 项目说明文档
```

## 🎨 自定义配置

### 添加新产品

在 `worker.js` 中的 `products` 数组中添加新产品：

```javascript
{
  id: 'your-product-id',
  name: '产品名称',
  description: '产品描述',
  icon: '🎯',
  tags: ['标签1', '标签2'],
  githubUrl: 'https://github.com/user/repo', // 可选
  features: [
    '功能1',
    '功能2'
  ],
  techStack: ['技术栈'], // 可选
  category: '分类',     // 可选
  difficulty: '难度'    // 可选
}
```

### 修改样式

编辑 `styles.css` 文件中的 CSS 变量：

```css
:root {
    --primary-color: #3b82f6;      /* 主色调 */
    --secondary-color: #64748b;     /* 次要颜色 */
    --background: #ffffff;          /* 背景色 */
    --surface: #f8fafc;             /* 表面色 */
    --text-primary: #1e293b;        /* 主文字色 */
    --text-secondary: #64748b;      /* 次要文字色 */
}
```

### 自定义域名

1. 在 Cloudflare Dashboard 中添加你的域名
2. 配置 DNS 记录指向 Workers
3. 修改 `wrangler.toml` 中的域名配置
4. 重新部署 Workers

## 📊 性能优化

- **静态资源缓存**: Workers 自动处理静态文件缓存
- **全球CDN**: Cloudflare 全球网络加速
- **压缩优化**: 自动压缩 HTML, CSS, JS
- **图片优化**: 支持现代图片格式 (WebP, AVIF)

## 🔧 故障排除

### 常见问题

1. **部署失败**
   - 检查 `wrangler.toml` 配置是否正确
   - 确认已正确登录 Cloudflare 账号
   - 检查 Workers 配额是否充足

2. **样式显示异常**
   - 检查 CSS 文件路径是否正确
   - 确认 Cloudflare Workers 路由配置
   - 检查浏览器控制台错误信息

3. **产品详情页 404**
   - 确认产品 ID 配置正确
   - 检查 Workers 脚本路由逻辑
   - 验证 URL 格式是否正确

### 调试模式

```bash
wrangler dev --local
```

启用本地调试模式，查看详细日志信息。

## 📝 许可证

MIT License - 详见 [LICENSE](LICENSE) 文件

## 🤝 贡献指南

1. Fork 本仓库
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 创建 Pull Request

## 📞 联系方式

- **邮箱**: contact@example.com
- **GitHub**: [你的 GitHub](https://github.com/yourusername)
- **项目主页**: [项目地址](https://your-domain.com)

## 🌟 致谢

感谢以下开源项目和服务：

- [Cloudflare Workers](https://workers.cloudflare.com/) - 部署平台
- [Wrangler](https://github.com/cloudflare/wrangler) - CLI 工具
- [liangdabiao/ai-make-face-meme](https://github.com/liangdabiao/ai-make-face-meme) - AI 面包表情生成器项目

---

⭐ 如果这个项目对你有帮助，请给个 Star！